flyway {
    url = 'jdbc:mysql://localhost:3306/'
    user = 'root'
    password = ''
    schemas = ['reporting']
    locations = ['filesystem:' + rootDir.path +'/reporting/sql']
    driver = 'com.mysql.jdbc.Driver'
}
